- name: Vérifier le contenu de /tmp/install.sh
  shell: cat /tmp/install.sh
  register: install_script_content
  ignore_errors: yes

- name: Afficher le contenu de /tmp/install.sh
  debug:
    var: install_script_content.stdout_lines
