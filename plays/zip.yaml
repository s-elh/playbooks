---
- name: Installer kURL
  hosts: all
  become: yes
  vars:
    kurl_url: "https://kurl.sh/9420c3b"

  tasks:
    - name: Télécharger et exécuter le script d'installation kURL
      shell: |
        yes | curl {{ kurl_url }} | sudo bash
      args:
        executable: /bin/bash
